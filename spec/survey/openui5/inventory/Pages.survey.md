# Pages — OpenUI5 survey

Scope: [`scopes/Pages/scope.md`](../../../scopes/Pages/scope.md). Full survey
of the Pages category against the pinned OpenUI5 commit — 0 matched
classes. Dashboard, Shell page, and Empty page are predefined page
*layouts/compositions* (routing/navigation + content conventions), not
individual OpenUI5 classes, so no class in the surveyed library matches
any of them directly; app-level page composition in OpenUI5 is typically
built from Containers/Views/Widgets objects already covered in those
files (e.g. `sap.m.Page`, `sap.tnt.ToolPage`).

## Dashboard

*Predefined page layout for overview metrics/summary content.* No OpenUI5
class matched this object.

## Shell page

*Page with no content, connects routing and navigation.* No OpenUI5 class
matched this object.

## Empty page

*Page with no content and no routing/navigation.* No OpenUI5 class matched
this object.
